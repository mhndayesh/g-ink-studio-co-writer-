{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "arq app.workers.export_worker.WorkerSettings",
    "restartPolicyType": "ALWAYS"
  }
}
