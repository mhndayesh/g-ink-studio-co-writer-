# Copy to .env.local and fill in. .env.local is gitignored.

# Browser → FastAPI base URL. The web app talks to the API at this origin.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080

# Optional: CDN/base URL for uploaded cover images (leave blank to serve from the API).
# NEXT_PUBLIC_CDN_URL=
